extends layout

block content
  h1= title
  - var result = results 
  div.container-fluid
     div.row
        each item,index in result
            div.col-sm-2(onclick=`buy(${index})`) truck  Name: #{item["truck_name"]}  <br>  truck_size: #{item["truck_size"]} <br> truck_type: #{item["truck_type"]} 
  script.
      function buy(index){
         alert("you purchased item "+(index+1))
      }